spring:
  datasource:
    url: jdbc:mysql://192.168.1.69:3306/cl?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
  liquibase:
    change-log: db/liquibase.xml
    enabled: true

cloud:
  aws:
    region:
      static: us-east-2
